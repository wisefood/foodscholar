fastapi==0.118.0
langchain==0.3.27
langchain-groq==0.3.8
uvicorn==0.37.0
elasticsearch==8.14.0
pydantic==2.9.1
pydantic-settings==2.5.2
redis==6.4.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
minio==7.2.18
httpx==0.27.0
json5==0.9.25
sentence-transformers==5.2.3
transformers==4.49.0
# 0.0.28 carries `wisefood_mcp` with the Phase 2 pipeline tools; 0.0.27 was
# the first release with the package at all. Deliberately NOT the [mcp] extra: that
# pulls the MCP SDK, which needs starlette>=1.0 and would fight FastAPI here.
# Only the standalone `wisefood-mcp` server process needs that extra.
wisefood>=0.0.28
# The integrator's agent loop drives tool-calling through the raw Groq SDK —
# langchain-groq pulls it in, but a module that imports it should say so.
groq>=0.30
openai>=1.0.0,<2.0.0
PyMuPDF>=1.24.0,<2.0.0
python-igraph==1.0.0
scispacy==0.6.2
pyarrow==23.0.1
numpy==1.26.4
pandas==3.0.1
langfuse>=3.0.0,<4.0.0
